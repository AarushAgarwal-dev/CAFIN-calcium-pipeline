# Complete CAFIN environment: GUI, AI interpretation, paper generation, and reproduction.
#
# Python is installed separately, not through pip.
# Required Python: 3.10 to 3.12 (3.11 recommended).
# Install this file with: python -m pip install -r requirements.txt
#
# For a GPU-aware installation, prefer:
#   python install.py --with-ai --with-paper --launch

# Basic Python package tools
pip>=23
setuptools>=68
wheel>=0.41

# Core scientific computing and plots
numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11,<2
matplotlib>=3.8,<4
scikit-learn>=1.3,<2

# Images and microscopy data
opencv-python>=4.8,<5
scikit-image>=0.22,<0.26
tifffile>=2023.9
imageio>=2.31,<3
pillow>=10,<13

# Interactive GUI
streamlit>=1.40,<2
plotly>=5.18,<7

# Segmentation, registration, and tracking
# Generic PyTorch is CPU-compatible. install.py selects CUDA, DirectML, ROCm,
# or Apple Silicon support when the hardware and operating system support it.
torch>=2.2,<3
cellpose>=3.1,<5
itk-elastix>=0.20,<1
SimpleITK>=2.3,<3

# Optional AI interpretation through Amazon Bedrock
boto3>=1.34,<2

# Paper generation and reproducibility
python-docx>=1.1,<2
