# Minimal environment to reproduce the CAFIN calcium-transient study.
# Python 3.10+ recommended.

# --- analysis + figures + paper (all that reproduce.py / generate_paper.py need) ---
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
python-docx>=1.1

# --- only needed to RE-RUN the full raw-image pipeline (cafin_pipeline.py) ---
opencv-python>=4.8
scikit-image>=0.21
tifffile>=2023.7
imageio>=2.31
SimpleITK>=2.3
cellpose>=3.0
# torch is pulled in by cellpose; for GPU install a CUDA build, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

# --- only needed to run the interactive GUI (cafin_gui.py) ---
streamlit>=1.30
plotly>=5.18          # ROI drag-box selector + interactive plots
scikit-learn>=1.3     # PCA + K-means trace clustering
itk-elastix>=0.20     # 'elastic' registration method (non-rigid B-spline)
# scipy (above) also powers the segment-every-frame cell tracker (cafin_track.py)
